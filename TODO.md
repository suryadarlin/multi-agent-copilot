# TODO

## Frontend/Backend mismatch: /students payload & response
- [x] Inspect `main.py` StudentIn model and /students endpoints.
- [x] Update `StudentIn` to include `roll_no` and `department`.
- [x] Ensure `create_student()` stores `{id, name, roll_no, department}`.
- [x] Ensure `GET /students` returns full student objects (id/name/roll_no/department).

- [ ] Run tests to verify behavior.


